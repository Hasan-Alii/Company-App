@model EmployeeViewModel

@{
    ViewData["Title"] = $"{Model.Name} Details";
}

<h1>@Model.Name Details</h1>

<br />
<br />

<div>
    <partial name="EmployeePartialViews/DetailsDeleteEmployeePartialView" model="Model" />

    <br />
    <a asp-action="Update" asp-route-Id="@Model.Id" class="btn btn-success">Update Employee</a>
    <a asp-action="Index" class="btn btn-outline-dark">Back To Employees List</a>
</div>

